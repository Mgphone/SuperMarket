const AreaManagerPortal = () => {
  // Message for the area manager
  const welcomeMessage =
    "Welcome, Area Manager! You can manage branches, users, and view sales data.";

  return (
    <div>
      <h1>Area Manager Portal</h1>
      <p>{welcomeMessage}</p>
    </div>
  );
};

export default AreaManagerPortal;
